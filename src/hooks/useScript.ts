import { useState, useEffect } from "react"

let cachedScripts = [];

let useLincxScript = (zoneId: string) => {
  let [state, setState] = useState({
    loaded: false,
    error: false,
  })


  useEffect(() => {
    if (cachedScripts.in)
  })

}
